"use client";

import { BuyListingsView } from "@/components/BuyListingsView";

/** @deprecated Use BuyListingsView on /buy — kept for backward compatibility */
export function PortalView() {
  return <BuyListingsView />;
}
